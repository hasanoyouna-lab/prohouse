# Pro House — ملف سياق النظام لأي مساعد ذكاء اصطناعي

هذا الملف يشرح نظام إدارة العمليات في Pro House: ماذا يقيس، وكيف تُحسب أرقامه، وأين حدود الثقة فيها.
ارفعه مع ملف البيانات المصدَّر (`*-ai.json`) قبل طلب أي تحليل.

---

## ١. المنشأة

- **مطعم Pro House** — ثلاثة فروع: **الروضة**، **الشاطئ**، **عبداللطيف جميل**.
- كل فرع مستقل تشغيلياً: يستلم إنتاجه من المطبخ المركزي، ويجرد بنفسه، ويطلب لليوم التالي بنفسه.
- الأدوار: **مالك** (كل شيء) / **مدير فرع** (فرعه فقط) / **شيف** (يرى الكل ولا يعدّل) / **موظف** (فرعه، إدخال فقط).

## ٢. الدورة اليومية

1. **افتتاح الفرع** — قائمة فحص صباحية وتوثيق بصري.
2. **الاستلام** — الفرع يستلم من المطبخ ويسجّل الوزن الفعلي مقابل المطلوب.
3. **البيع** — يُسجَّل في نظام الكاشير (Tabsense)، ويُسحب آلياً كل ليلة.
4. **الجرد والمتبقي** — وزن ما تبقّى فعلاً آخر اليوم، ومقارنته بالمتوقع.
5. **الهدر** — يُسجَّل بسببه المحدد.
6. **طلبية الغد** — تُرسل للمطبخ، وتصبح "المطلوب" في استلام الغد.
7. **إغلاق الفرع** — اعتماد اليوم.

## ٣. قواعد الحساب — ثابتة ولا يجوز تغييرها في أي تحليل

| القاعدة | القيمة | لماذا |
|---|---|---|
| **الوجبة** | ‎١٥٠‎ جرام | كل تحويل بين الوزن وعدد الوجبات يمر بهذا الرقم |
| **التصنيفات الموزونة** | دجاج، لحم، بحري، ساندويتشات | غيرها يُعدّ بالحبة أو الطاسة، لا بالوزن |
| **أم علي** | حبتان = ساندويتش واحد | يُسجَّل في الكاشير بالحبة، ويُقسم ÷٢ ويُضاف للساندويتشات |
| **استقلال الفروع** | مطلق | لا يُجمع فرعان في رقم واحد إلا بطلب صريح؛ لكل فرع نمط استهلاك مختلف |
| **انحراف المتبقي** | `المتوقع = المستلم − (المباع × ١٥٠)` | و`الانحراف = الفعلي − المتوقع`. سالب = فقد غير مفسّر |
| **جرد العصيرات** | `المتوقع = افتتاحي + إضافات − مبيعات` | الافتتاحي = إقفال أمس. سالب = نقص |

**أعتاب التنبيه الحالية** (قابلة للضبط من شاشة الإعدادات، وهي محل مراجعة):
نقص ‎−٢٠٪‎ · زيادة ‎+٢٥٪‎ · إرجاع ‎٣٠٪‎ · انحراف المتبقي: طبيعي ≤‎٥٪‎، تنبيه ≤‎١٥٪‎، حرج فوقها.

## ٤. جداول البيانات في الملف المصدَّر

| الجدول | المحتوى | الحقول المفتاحية |
|---|---|---|
| `items` | كتالوج الأصناف | `id, category, name, unit, branches, sortOrder` |
| `dailyEntries` | الاستلام والإرجاع لكل صنف/يوم/فرع | `date, branch, itemId, received, returned, confirmed, notes` |
| `dayMeta` | من سجّل اليوم وروابط تقاريره | `date, branch, employeeName` |
| `tomorrowOrders` | الطلبيات المرسلة للمطبخ | `date, branch, itemId, qty, notes` |
| `tabsenseSales` | المبيعات حسب التصنيف من الكاشير | `date, branch, category, qty` |
| `juices` / `juiceCounts` / `juiceSales` | كتالوج العصيرات وجردها ومبيعاتها | `date, branch, opening, added, sold, counted` |
| `employees` | الأسماء والأدوار والفروع | `name, role, branches` |
| `settings` | الإعدادات التشغيلية | `key, value` |

**الوحدات:** `received` و`returned` و`remainingWeight` بالجرام. `qty` في `tomorrowOrders` بوحدة الصنف نفسه (`items.unit`). `qty` في `tabsenseSales` عدد وجبات مباعة.

**الربط:** `dailyEntries.itemId` ↔ `items.id`. طلبية يوم ما تصبح "المطلوب" في استلام **اليوم التالي** لنفس الفرع.

## ٥. حدود الثقة في البيانات — اقرأها قبل أي استنتاج

هذه ليست تحفظات شكلية؛ تجاهلها ينتج توصيات خاطئة:

1. **سحب المبيعات من الكاشير متعثّر حالياً** — السكربت يعمل بجدولته لكنه يعيد جدولاً فارغاً في أيام عديدة. أي يوم قيمة مبيعاته صفر قد يكون **يوم إغلاق فعلي أو فشل سحب**. تحقّق من وجود استلام في نفس اليوم قبل اعتباره يوماً بلا مبيعات.
2. **الإدخال قد يكون جزئياً** — الحفظ تلقائي، فقد يُحفظ يوم نصف مكتمل. `confirmed` هو مؤشر الاكتمال، لا وجود الصف.
3. **العصيرات حديثة** — الشاشة أُطلقت في أغسطس ٢٠٢٦، فالسجل التاريخي قصير.
4. **الهدر يعتمد على أمانة التسجيل** — انخفاض المسجَّل لا يعني انخفاض الهدر الحقيقي؛ قارنه دائماً بانحراف المتبقي المحسوب.
5. **الفروع بدأت في أوقات مختلفة** — لا تقارن إجمالياتها المطلقة، قارن معدلاتها.

## ٦. الكود

مستودع عام: `https://github.com/hasanoyouna-lab/prohouse`

- واجهة HTML/CSS/JavaScript بلا أي إطار عمل أو خطوة بناء.
- الباك اند ملف واحد `apps-script/Code.gs` على Google Apps Script فوق Google Sheet.
- `tabsense-sync/` سكربت Node يسحب المبيعات ليلياً.
- قواعد الحساب أعلاه مكتوبة في: `js/shared.js` (الوجبة والتصنيفات) و`js/remaining.js` (الانحراف) و`js/juices.js` (معادلة العصيرات).

## ٧. ما يمكن تنفيذه فعلياً

عند اقتراح تحسين، هذه الأشكال قابلة للتطبيق مباشرة:

- **أعتاب ونِسب** → تُضبط من شاشة الإعدادات فوراً بلا برمجة.
- **مستويات طلب قياسية لكل صنف/فرع/يوم أسبوع** → تُغذّي محرك اقتراح طلبية الغد.
- **قواعد تنبيه** (متى نرسل إشعار واتساب ولمن) → تُضاف للباك اند.
- **تقرير جديد** → يُبنى إن كانت حقوله موجودة في الجداول أعلاه.
- **حقل إدخال جديد** → ممكن، لكنه يعني تدريب الموظفين، فبرّره بعائد واضح.
